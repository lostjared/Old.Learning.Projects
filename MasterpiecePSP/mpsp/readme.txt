This works with the 2.0 Eboot loader using the tif exploit
also works on 1.50

to compile ( if you have the PSPSDK && SDL )

make pcc
make

to compile for pc ( if you have SDL )
make pc


- Jared