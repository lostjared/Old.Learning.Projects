/*

  MasterPiece written utilizing MasterX SDK
  written by Jared Bruni


  www.lostsidedead.com


  Open Source, Open Mind

   "I am only as I think and believe"


*/

#include "thehead.h"



void Game::drawgame()
{


/*
	int STARTPOSX = STARTX;
    int STARTPOSY = STARTY;
    int x = STARTPOSX,y = STARTPOSY;

	for(int i = 0; i < 17; i++)
	{


		for(int j = 0; j < 8; j++)
		{
     
        grid_blocks[grid.Grid[i][j]].DisplayGraphic(x,y);
			x = x + 32;

		}
          x = STARTPOSX;
		  y = y + 16;
		// down lower
	}

	// get cordiantes for
	
	int dx,dy;

	getcords(grid.block.y1,grid.block.x,dx,dy); if(dx == -1) { dx = 0; dy = 0; }
	grid_blocks[grid.block.color.c1].DisplayGraphic(dx,dy);
	getcords(grid.block.y2,grid.block.x,dx,dy); if(dx == -1) { dx = 0; dy = 0; }
	grid_blocks[grid.block.color.c2].DisplayGraphic(dx,dy);
	getcords(grid.block.y3,grid.block.x,dx,dy); if(dx == -1) { dx = 0; dy = 0; }
	grid_blocks[grid.block.color.c3].DisplayGraphic(dx,dy);

 

	int bx = 510; int by = 200;
 
	grid_blocks[grid.block2.color.c1].DisplayGraphic(bx,by);
	grid_blocks[grid.block2.color.c2].DisplayGraphic(bx,by+16);
	grid_blocks[grid.block2.color.c3].DisplayGraphic(bx,by+32);

	// drawing the score , and  lines

	mxhwnd.text.setfont(mfonts[0]);
	mxhwnd.text.settextcolor( RGB(rand()%255,rand()%255,rand()%255) );
	mxhwnd.text.setbkcolor( 0x0 );
	mxhwnd.text.sprinttext(200,60,"Score: %i", grid.Game.score);
	mxhwnd.text.sprinttext(310,60,"Lines: %i", grid.Game.lines);

*/


}